import { callConsoleMessage } from './utils.js';
export const Initialize = function (headMessage = 'Init:') {
    return function LoggerMethod(originalMethod, _context) {
        const replacementMethod = function (...args) {
            const result = originalMethod.call(this, ...args);
            const version = args[1];
            callConsoleMessage.call(this, headMessage, `LIDO SDK ${version}  `);
            return result;
        };
        return replacementMethod;
    };
};
//# sourceMappingURL=Initialize.js.map