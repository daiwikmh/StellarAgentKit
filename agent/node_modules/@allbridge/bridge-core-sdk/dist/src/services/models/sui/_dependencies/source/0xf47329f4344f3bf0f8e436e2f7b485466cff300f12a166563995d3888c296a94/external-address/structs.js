"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ExternalAddress = void 0;
exports.isExternalAddress = isExternalAddress;
// @ts-nocheck
const reified_1 = require("../../../../_framework/reified");
const util_1 = require("../../../../_framework/util");
const structs_1 = require("../bytes32/structs");
const index_1 = require("../index");
const bcs_1 = require("@mysten/sui/bcs");
const utils_1 = require("@mysten/sui/utils");
/* ============================== ExternalAddress =============================== */
function isExternalAddress(type) {
    type = (0, util_1.compressSuiType)(type);
    return type === `${index_1.PKG_V1}::external_address::ExternalAddress`;
}
class ExternalAddress {
    __StructClass = true;
    static get $typeName() {
        return `${index_1.PKG_V1}::external_address::ExternalAddress`;
    }
    static $numTypeParams = 0;
    static $isPhantom = [];
    $typeName = ExternalAddress.$typeName;
    $fullTypeName;
    $typeArgs;
    $isPhantom = ExternalAddress.$isPhantom;
    value;
    constructor(typeArgs, fields) {
        this.$fullTypeName = (0, util_1.composeSuiType)(ExternalAddress.$typeName, ...typeArgs);
        this.$typeArgs = typeArgs;
        this.value = fields.value;
    }
    static reified() {
        return {
            typeName: ExternalAddress.$typeName,
            fullTypeName: (0, util_1.composeSuiType)(ExternalAddress.$typeName, ...[]),
            typeArgs: [],
            isPhantom: ExternalAddress.$isPhantom,
            reifiedTypeArgs: [],
            fromFields: (fields) => ExternalAddress.fromFields(fields),
            fromFieldsWithTypes: (item) => ExternalAddress.fromFieldsWithTypes(item),
            fromBcs: (data) => ExternalAddress.fromBcs(data),
            bcs: ExternalAddress.bcs,
            fromJSONField: (field) => ExternalAddress.fromJSONField(field),
            fromJSON: (json) => ExternalAddress.fromJSON(json),
            fromSuiParsedData: (content) => ExternalAddress.fromSuiParsedData(content),
            fromSuiObjectData: (content) => ExternalAddress.fromSuiObjectData(content),
            fetch: async (client, id) => ExternalAddress.fetch(client, id),
            new: (fields) => {
                return new ExternalAddress([], fields);
            },
            kind: "StructClassReified",
        };
    }
    static get r() {
        return ExternalAddress.reified();
    }
    static phantom() {
        return (0, reified_1.phantom)(ExternalAddress.reified());
    }
    static get p() {
        return ExternalAddress.phantom();
    }
    static get bcs() {
        return bcs_1.bcs.struct("ExternalAddress", {
            value: structs_1.Bytes32.bcs,
        });
    }
    static fromFields(fields) {
        return ExternalAddress.reified().new({
            value: (0, reified_1.decodeFromFields)(structs_1.Bytes32.reified(), fields.value),
        });
    }
    static fromFieldsWithTypes(item) {
        if (!isExternalAddress(item.type)) {
            throw new Error("not a ExternalAddress type");
        }
        return ExternalAddress.reified().new({
            value: (0, reified_1.decodeFromFieldsWithTypes)(structs_1.Bytes32.reified(), item.fields.value),
        });
    }
    static fromBcs(data) {
        return ExternalAddress.fromFields(ExternalAddress.bcs.parse(data));
    }
    toJSONField() {
        return {
            value: this.value.toJSONField(),
        };
    }
    toJSON() {
        return {
            $typeName: this.$typeName,
            $typeArgs: this.$typeArgs,
            ...this.toJSONField(),
        };
    }
    static fromJSONField(field) {
        return ExternalAddress.reified().new({
            value: (0, reified_1.decodeFromJSONField)(structs_1.Bytes32.reified(), field.value),
        });
    }
    static fromJSON(json) {
        if (json.$typeName !== ExternalAddress.$typeName) {
            throw new Error("not a WithTwoGenerics json object");
        }
        return ExternalAddress.fromJSONField(json);
    }
    static fromSuiParsedData(content) {
        if (content.dataType !== "moveObject") {
            throw new Error("not an object");
        }
        if (!isExternalAddress(content.type)) {
            throw new Error(`object at ${content.fields.id} is not a ExternalAddress object`);
        }
        return ExternalAddress.fromFieldsWithTypes(content);
    }
    static fromSuiObjectData(data) {
        if (data.bcs) {
            if (data.bcs.dataType !== "moveObject" || !isExternalAddress(data.bcs.type)) {
                throw new Error(`object at is not a ExternalAddress object`);
            }
            return ExternalAddress.fromBcs((0, utils_1.fromB64)(data.bcs.bcsBytes));
        }
        if (data.content) {
            return ExternalAddress.fromSuiParsedData(data.content);
        }
        throw new Error("Both `bcs` and `content` fields are missing from the data. Include `showBcs` or `showContent` in the request.");
    }
    static async fetch(client, id) {
        const res = await client.getObject({ id, options: { showBcs: true } });
        if (res.error) {
            throw new Error(`error fetching ExternalAddress object at id ${id}: ${res.error.code}`);
        }
        if (res.data?.bcs?.dataType !== "moveObject" || !isExternalAddress(res.data.bcs.type)) {
            throw new Error(`object at id ${id} is not a ExternalAddress object`);
        }
        return ExternalAddress.fromSuiObjectData(res.data);
    }
}
exports.ExternalAddress = ExternalAddress;
//# sourceMappingURL=structs.js.map