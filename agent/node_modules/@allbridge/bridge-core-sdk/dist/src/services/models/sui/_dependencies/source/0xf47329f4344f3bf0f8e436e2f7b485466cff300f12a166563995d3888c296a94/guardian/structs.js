"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Guardian = void 0;
exports.isGuardian = isGuardian;
// @ts-nocheck
const reified_1 = require("../../../../_framework/reified");
const util_1 = require("../../../../_framework/util");
const structs_1 = require("../bytes20/structs");
const index_1 = require("../index");
const bcs_1 = require("@mysten/sui/bcs");
const utils_1 = require("@mysten/sui/utils");
/* ============================== Guardian =============================== */
function isGuardian(type) {
    type = (0, util_1.compressSuiType)(type);
    return type === `${index_1.PKG_V1}::guardian::Guardian`;
}
class Guardian {
    __StructClass = true;
    static get $typeName() {
        return `${index_1.PKG_V1}::guardian::Guardian`;
    }
    static $numTypeParams = 0;
    static $isPhantom = [];
    $typeName = Guardian.$typeName;
    $fullTypeName;
    $typeArgs;
    $isPhantom = Guardian.$isPhantom;
    pubkey;
    constructor(typeArgs, fields) {
        this.$fullTypeName = (0, util_1.composeSuiType)(Guardian.$typeName, ...typeArgs);
        this.$typeArgs = typeArgs;
        this.pubkey = fields.pubkey;
    }
    static reified() {
        return {
            typeName: Guardian.$typeName,
            fullTypeName: (0, util_1.composeSuiType)(Guardian.$typeName, ...[]),
            typeArgs: [],
            isPhantom: Guardian.$isPhantom,
            reifiedTypeArgs: [],
            fromFields: (fields) => Guardian.fromFields(fields),
            fromFieldsWithTypes: (item) => Guardian.fromFieldsWithTypes(item),
            fromBcs: (data) => Guardian.fromBcs(data),
            bcs: Guardian.bcs,
            fromJSONField: (field) => Guardian.fromJSONField(field),
            fromJSON: (json) => Guardian.fromJSON(json),
            fromSuiParsedData: (content) => Guardian.fromSuiParsedData(content),
            fromSuiObjectData: (content) => Guardian.fromSuiObjectData(content),
            fetch: async (client, id) => Guardian.fetch(client, id),
            new: (fields) => {
                return new Guardian([], fields);
            },
            kind: "StructClassReified",
        };
    }
    static get r() {
        return Guardian.reified();
    }
    static phantom() {
        return (0, reified_1.phantom)(Guardian.reified());
    }
    static get p() {
        return Guardian.phantom();
    }
    static get bcs() {
        return bcs_1.bcs.struct("Guardian", {
            pubkey: structs_1.Bytes20.bcs,
        });
    }
    static fromFields(fields) {
        return Guardian.reified().new({
            pubkey: (0, reified_1.decodeFromFields)(structs_1.Bytes20.reified(), fields.pubkey),
        });
    }
    static fromFieldsWithTypes(item) {
        if (!isGuardian(item.type)) {
            throw new Error("not a Guardian type");
        }
        return Guardian.reified().new({
            pubkey: (0, reified_1.decodeFromFieldsWithTypes)(structs_1.Bytes20.reified(), item.fields.pubkey),
        });
    }
    static fromBcs(data) {
        return Guardian.fromFields(Guardian.bcs.parse(data));
    }
    toJSONField() {
        return {
            pubkey: this.pubkey.toJSONField(),
        };
    }
    toJSON() {
        return {
            $typeName: this.$typeName,
            $typeArgs: this.$typeArgs,
            ...this.toJSONField(),
        };
    }
    static fromJSONField(field) {
        return Guardian.reified().new({
            pubkey: (0, reified_1.decodeFromJSONField)(structs_1.Bytes20.reified(), field.pubkey),
        });
    }
    static fromJSON(json) {
        if (json.$typeName !== Guardian.$typeName) {
            throw new Error("not a WithTwoGenerics json object");
        }
        return Guardian.fromJSONField(json);
    }
    static fromSuiParsedData(content) {
        if (content.dataType !== "moveObject") {
            throw new Error("not an object");
        }
        if (!isGuardian(content.type)) {
            throw new Error(`object at ${content.fields.id} is not a Guardian object`);
        }
        return Guardian.fromFieldsWithTypes(content);
    }
    static fromSuiObjectData(data) {
        if (data.bcs) {
            if (data.bcs.dataType !== "moveObject" || !isGuardian(data.bcs.type)) {
                throw new Error(`object at is not a Guardian object`);
            }
            return Guardian.fromBcs((0, utils_1.fromB64)(data.bcs.bcsBytes));
        }
        if (data.content) {
            return Guardian.fromSuiParsedData(data.content);
        }
        throw new Error("Both `bcs` and `content` fields are missing from the data. Include `showBcs` or `showContent` in the request.");
    }
    static async fetch(client, id) {
        const res = await client.getObject({ id, options: { showBcs: true } });
        if (res.error) {
            throw new Error(`error fetching Guardian object at id ${id}: ${res.error.code}`);
        }
        if (res.data?.bcs?.dataType !== "moveObject" || !isGuardian(res.data.bcs.type)) {
            throw new Error(`object at id ${id} is not a Guardian object`);
        }
        return Guardian.fromSuiObjectData(res.data);
    }
}
exports.Guardian = Guardian;
//# sourceMappingURL=structs.js.map