"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MintAllowance = void 0;
exports.isMintAllowance = isMintAllowance;
// @ts-nocheck
const reified_1 = require("../../../../_framework/reified");
const util_1 = require("../../../../_framework/util");
const index_1 = require("../index");
const bcs_1 = require("@mysten/sui/bcs");
const utils_1 = require("@mysten/sui/utils");
/* ============================== MintAllowance =============================== */
function isMintAllowance(type) {
    type = (0, util_1.compressSuiType)(type);
    return type.startsWith(`${index_1.PKG_V1}::mint_allowance::MintAllowance` + "<");
}
class MintAllowance {
    __StructClass = true;
    static get $typeName() {
        return `${index_1.PKG_V1}::mint_allowance::MintAllowance`;
    }
    static $numTypeParams = 1;
    static $isPhantom = [true];
    $typeName = MintAllowance.$typeName;
    $fullTypeName;
    $typeArgs;
    $isPhantom = MintAllowance.$isPhantom;
    value;
    constructor(typeArgs, fields) {
        this.$fullTypeName = (0, util_1.composeSuiType)(MintAllowance.$typeName, ...typeArgs);
        this.$typeArgs = typeArgs;
        this.value = fields.value;
    }
    static reified(T) {
        return {
            typeName: MintAllowance.$typeName,
            fullTypeName: (0, util_1.composeSuiType)(MintAllowance.$typeName, ...[(0, reified_1.extractType)(T)]),
            typeArgs: [(0, reified_1.extractType)(T)],
            isPhantom: MintAllowance.$isPhantom,
            reifiedTypeArgs: [T],
            fromFields: (fields) => MintAllowance.fromFields(T, fields),
            fromFieldsWithTypes: (item) => MintAllowance.fromFieldsWithTypes(T, item),
            fromBcs: (data) => MintAllowance.fromBcs(T, data),
            bcs: MintAllowance.bcs,
            fromJSONField: (field) => MintAllowance.fromJSONField(T, field),
            fromJSON: (json) => MintAllowance.fromJSON(T, json),
            fromSuiParsedData: (content) => MintAllowance.fromSuiParsedData(T, content),
            fromSuiObjectData: (content) => MintAllowance.fromSuiObjectData(T, content),
            fetch: async (client, id) => MintAllowance.fetch(client, T, id),
            new: (fields) => {
                return new MintAllowance([(0, reified_1.extractType)(T)], fields);
            },
            kind: "StructClassReified",
        };
    }
    static get r() {
        return MintAllowance.reified;
    }
    static phantom(T) {
        return (0, reified_1.phantom)(MintAllowance.reified(T));
    }
    static get p() {
        return MintAllowance.phantom;
    }
    static get bcs() {
        return bcs_1.bcs.struct("MintAllowance", {
            value: bcs_1.bcs.u64(),
        });
    }
    static fromFields(typeArg, fields) {
        return MintAllowance.reified(typeArg).new({
            value: (0, reified_1.decodeFromFields)("u64", fields.value),
        });
    }
    static fromFieldsWithTypes(typeArg, item) {
        if (!isMintAllowance(item.type)) {
            throw new Error("not a MintAllowance type");
        }
        (0, reified_1.assertFieldsWithTypesArgsMatch)(item, [typeArg]);
        return MintAllowance.reified(typeArg).new({
            value: (0, reified_1.decodeFromFieldsWithTypes)("u64", item.fields.value),
        });
    }
    static fromBcs(typeArg, data) {
        return MintAllowance.fromFields(typeArg, MintAllowance.bcs.parse(data));
    }
    toJSONField() {
        return {
            value: this.value.toString(),
        };
    }
    toJSON() {
        return {
            $typeName: this.$typeName,
            $typeArgs: this.$typeArgs,
            ...this.toJSONField(),
        };
    }
    static fromJSONField(typeArg, field) {
        return MintAllowance.reified(typeArg).new({
            value: (0, reified_1.decodeFromJSONField)("u64", field.value),
        });
    }
    static fromJSON(typeArg, json) {
        if (json.$typeName !== MintAllowance.$typeName) {
            throw new Error("not a WithTwoGenerics json object");
        }
        (0, reified_1.assertReifiedTypeArgsMatch)((0, util_1.composeSuiType)(MintAllowance.$typeName, (0, reified_1.extractType)(typeArg)), json.$typeArgs, [
            typeArg,
        ]);
        return MintAllowance.fromJSONField(typeArg, json);
    }
    static fromSuiParsedData(typeArg, content) {
        if (content.dataType !== "moveObject") {
            throw new Error("not an object");
        }
        if (!isMintAllowance(content.type)) {
            throw new Error(`object at ${content.fields.id} is not a MintAllowance object`);
        }
        return MintAllowance.fromFieldsWithTypes(typeArg, content);
    }
    static fromSuiObjectData(typeArg, data) {
        if (data.bcs) {
            if (data.bcs.dataType !== "moveObject" || !isMintAllowance(data.bcs.type)) {
                throw new Error(`object at is not a MintAllowance object`);
            }
            const gotTypeArgs = (0, util_1.parseTypeName)(data.bcs.type).typeArgs;
            if (gotTypeArgs.length !== 1) {
                throw new Error(`type argument mismatch: expected 1 type argument but got '${gotTypeArgs.length}'`);
            }
            const gotTypeArg = (0, util_1.compressSuiType)(gotTypeArgs[0]);
            const expectedTypeArg = (0, util_1.compressSuiType)((0, reified_1.extractType)(typeArg));
            if (gotTypeArg !== (0, util_1.compressSuiType)((0, reified_1.extractType)(typeArg))) {
                throw new Error(`type argument mismatch: expected '${expectedTypeArg}' but got '${gotTypeArg}'`);
            }
            return MintAllowance.fromBcs(typeArg, (0, utils_1.fromB64)(data.bcs.bcsBytes));
        }
        if (data.content) {
            return MintAllowance.fromSuiParsedData(typeArg, data.content);
        }
        throw new Error("Both `bcs` and `content` fields are missing from the data. Include `showBcs` or `showContent` in the request.");
    }
    static async fetch(client, typeArg, id) {
        const res = await client.getObject({ id, options: { showBcs: true } });
        if (res.error) {
            throw new Error(`error fetching MintAllowance object at id ${id}: ${res.error.code}`);
        }
        if (res.data?.bcs?.dataType !== "moveObject" || !isMintAllowance(res.data.bcs.type)) {
            throw new Error(`object at id ${id} is not a MintAllowance object`);
        }
        return MintAllowance.fromSuiObjectData(typeArg, res.data);
    }
}
exports.MintAllowance = MintAllowance;
//# sourceMappingURL=structs.js.map