"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Clock = void 0;
exports.isClock = isClock;
// @ts-nocheck
const reified_1 = require("../../_framework/reified");
const util_1 = require("../../_framework/util");
const index_1 = require("../index");
const structs_1 = require("../object/structs");
const bcs_1 = require("@mysten/sui/bcs");
const utils_1 = require("@mysten/sui/utils");
/* ============================== Clock =============================== */
function isClock(type) {
    type = (0, util_1.compressSuiType)(type);
    return type === `${index_1.PKG_V26}::clock::Clock`;
}
class Clock {
    __StructClass = true;
    static get $typeName() {
        return `${index_1.PKG_V26}::clock::Clock`;
    }
    static $numTypeParams = 0;
    static $isPhantom = [];
    $typeName = Clock.$typeName;
    $fullTypeName;
    $typeArgs;
    $isPhantom = Clock.$isPhantom;
    id;
    timestampMs;
    constructor(typeArgs, fields) {
        this.$fullTypeName = (0, util_1.composeSuiType)(Clock.$typeName, ...typeArgs);
        this.$typeArgs = typeArgs;
        this.id = fields.id;
        this.timestampMs = fields.timestampMs;
    }
    static reified() {
        return {
            typeName: Clock.$typeName,
            fullTypeName: (0, util_1.composeSuiType)(Clock.$typeName, ...[]),
            typeArgs: [],
            isPhantom: Clock.$isPhantom,
            reifiedTypeArgs: [],
            fromFields: (fields) => Clock.fromFields(fields),
            fromFieldsWithTypes: (item) => Clock.fromFieldsWithTypes(item),
            fromBcs: (data) => Clock.fromBcs(data),
            bcs: Clock.bcs,
            fromJSONField: (field) => Clock.fromJSONField(field),
            fromJSON: (json) => Clock.fromJSON(json),
            fromSuiParsedData: (content) => Clock.fromSuiParsedData(content),
            fromSuiObjectData: (content) => Clock.fromSuiObjectData(content),
            fetch: async (client, id) => Clock.fetch(client, id),
            new: (fields) => {
                return new Clock([], fields);
            },
            kind: "StructClassReified",
        };
    }
    static get r() {
        return Clock.reified();
    }
    static phantom() {
        return (0, reified_1.phantom)(Clock.reified());
    }
    static get p() {
        return Clock.phantom();
    }
    static get bcs() {
        return bcs_1.bcs.struct("Clock", {
            id: structs_1.UID.bcs,
            timestamp_ms: bcs_1.bcs.u64(),
        });
    }
    static fromFields(fields) {
        return Clock.reified().new({
            id: (0, reified_1.decodeFromFields)(structs_1.UID.reified(), fields.id),
            timestampMs: (0, reified_1.decodeFromFields)("u64", fields.timestamp_ms),
        });
    }
    static fromFieldsWithTypes(item) {
        if (!isClock(item.type)) {
            throw new Error("not a Clock type");
        }
        return Clock.reified().new({
            id: (0, reified_1.decodeFromFieldsWithTypes)(structs_1.UID.reified(), item.fields.id),
            timestampMs: (0, reified_1.decodeFromFieldsWithTypes)("u64", item.fields.timestamp_ms),
        });
    }
    static fromBcs(data) {
        return Clock.fromFields(Clock.bcs.parse(data));
    }
    toJSONField() {
        return {
            id: this.id,
            timestampMs: this.timestampMs.toString(),
        };
    }
    toJSON() {
        return {
            $typeName: this.$typeName,
            $typeArgs: this.$typeArgs,
            ...this.toJSONField(),
        };
    }
    static fromJSONField(field) {
        return Clock.reified().new({
            id: (0, reified_1.decodeFromJSONField)(structs_1.UID.reified(), field.id),
            timestampMs: (0, reified_1.decodeFromJSONField)("u64", field.timestampMs),
        });
    }
    static fromJSON(json) {
        if (json.$typeName !== Clock.$typeName) {
            throw new Error("not a WithTwoGenerics json object");
        }
        return Clock.fromJSONField(json);
    }
    static fromSuiParsedData(content) {
        if (content.dataType !== "moveObject") {
            throw new Error("not an object");
        }
        if (!isClock(content.type)) {
            throw new Error(`object at ${content.fields.id} is not a Clock object`);
        }
        return Clock.fromFieldsWithTypes(content);
    }
    static fromSuiObjectData(data) {
        if (data.bcs) {
            if (data.bcs.dataType !== "moveObject" || !isClock(data.bcs.type)) {
                throw new Error(`object at is not a Clock object`);
            }
            return Clock.fromBcs((0, utils_1.fromB64)(data.bcs.bcsBytes));
        }
        if (data.content) {
            return Clock.fromSuiParsedData(data.content);
        }
        throw new Error("Both `bcs` and `content` fields are missing from the data. Include `showBcs` or `showContent` in the request.");
    }
    static async fetch(client, id) {
        const res = await client.getObject({ id, options: { showBcs: true } });
        if (res.error) {
            throw new Error(`error fetching Clock object at id ${id}: ${res.error.code}`);
        }
        if (res.data?.bcs?.dataType !== "moveObject" || !isClock(res.data.bcs.type)) {
            throw new Error(`object at id ${id} is not a Clock object`);
        }
        return Clock.fromSuiObjectData(res.data);
    }
}
exports.Clock = Clock;
//# sourceMappingURL=structs.js.map