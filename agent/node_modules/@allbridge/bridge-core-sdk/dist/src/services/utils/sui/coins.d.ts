import { CoinStruct } from "@mysten/sui/client";
import { Transaction } from "@mysten/sui/transactions";
export declare function getCoinsWithAmounts(amounts: string[], coins: CoinStruct[], tx: Transaction): never[] | ({
    $kind: "Result";
    Result: number;
} & {
    $kind: "NestedResult";
    NestedResult: [number, number];
}[]);
