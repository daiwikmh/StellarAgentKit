import { SuiClient } from "@mysten/sui/client";
import { Transaction } from "@mysten/sui/transactions";
import { Reified, ToJSON, TypeArgument } from "../../models/sui/_framework/reified";
export declare function suiView<T extends TypeArgument>(client: SuiClient, tx: Transaction, reified: Reified<T, any>): Promise<ToJSON<T>>;
