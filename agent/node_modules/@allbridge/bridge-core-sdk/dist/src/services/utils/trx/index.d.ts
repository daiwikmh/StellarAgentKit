import { TronWeb } from "tronweb";
import { RawTransaction } from "../../models";
export declare function sendRawTransaction(tronWeb: TronWeb, rawTransaction: RawTransaction): Promise<{
    txId: string;
}>;
export declare function verifyTx(tronWeb: TronWeb, txId: string, timeout?: number): Promise<any>;
